#include "stdafx.h"

// FIXME: ????? who uses this?

/*#include "errordlg.h"

int globalTetMesherMessage(	const std::vector< std::string >& message,
							unsigned int style, 
							unsigned int contextID )
{
  assert(style == 0);
  assert(contextID == 0);

  CErrorDlg dlg( message );
  INT_PTR res = dlg.DoModal();

  return res;
}*/
